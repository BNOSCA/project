rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    function ownsUserDocument(uid) {
      return signedIn() && request.auth.uid == uid;
    }

    function payloadUserMatchesAuth() {
      return signedIn() && request.resource.data.user_id == request.auth.uid;
    }

    match /products/{productId} {
      allow read: if true;
      allow write: if false;
    }

    match /posts/{postId} {
      allow read: if true;
      allow write: if false;
    }

    match /creators/{creatorId} {
      allow read: if true;
      allow write: if false;
    }

    match /users/{uid} {
      allow read, create, update, delete: if ownsUserDocument(uid);
    }

    match /sessions/{sessionId} {
      allow read: if signedIn() && resource.data.user_id == request.auth.uid;
      allow create: if payloadUserMatchesAuth();
      allow update, delete: if signedIn()
        && resource.data.user_id == request.auth.uid
        && request.resource.data.user_id == request.auth.uid;
    }

    match /interactions/{eventId} {
      allow read: if signedIn() && resource.data.user_id == request.auth.uid;
      allow create: if payloadUserMatchesAuth();
      allow update, delete: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
