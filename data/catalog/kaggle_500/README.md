# Kaggle demo catalog

Contains 500 local JPEG images and their source metadata. Category counts: {'top': 167, 'bottom': 167, 'shoes': 166}.

All items are `demo_only`; the source CSV has no real price, stock status, or purchase URL. Serve `images/` at `/products/kaggle/` so `image_url` resolves correctly.
