# Core dependencies for FashionCLIP Embedding Pipeline & Hackathon
# Use the Transformers implementation of patrickjohncyh/fashion-clip. The
# legacy fashion-clip package pulls in an `annoy` extension that cannot build
# on the supported macOS toolchain.
torch>=2.2,<2.3
torchvision>=0.17,<0.18
transformers>=4.30,<4.47
pillow>=9.5.0
requests>=2.28.0
numpy>=1.24,<2
tqdm>=4.65.0
pydantic>=2.0.0
pytest>=7.0.0
